apiVersion: v1
kind: Service
metadata:
  name: {{ include "e-ren.fullname" . }}
  labels:
    {{- include "e-ren.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "e-ren.selectorLabels" . | nindent 4 }}
